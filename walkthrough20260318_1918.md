# Walkthrough — Estabilización Linux/Docker (Primera etapa)

**Fecha:** 2026-03-18 | **Archivos modificados:** 2

---

## Cambios aplicados

### 1 · Backslashes en escritura de JSON → [csv/modelo.php](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php) (~L 2623)

```diff
-        $fecha = "archivos_json\\" . date('Y-m-d-H_i_s');
-        if (file_exists("fc_json.txt")) {
-            $archivo = fopen("archivos_json\\fc_json.json", "a");
-            fwrite($archivo, PHP_EOL . "$data_string $formateado");
-            fclose($archivo);
-        } else {
-            $archivo = fopen($fecha . ".json", "w");
-            fwrite($archivo, PHP_EOL . "$data_string");
-            fclose($archivo);
-        }
+        /* Hardening Linux: separador compatible y mkdir preventivo */
+        $dir_json = 'archivos_json';
+        if (!is_dir($dir_json)) {
+            mkdir($dir_json, 0755, true);
+        }
+        $fecha = $dir_json . '/' . date('Y-m-d-H_i_s');
+        //
+        if (file_exists("fc_json.txt")) {
+            $fh_json = fopen($dir_json . '/fc_json.json', 'a');
+            if ($fh_json !== false) {
+                fwrite($fh_json, PHP_EOL . "$data_string $formateado");
+                fclose($fh_json);
+            }
+        } else {
+            $fh_json = fopen($fecha . '.json', 'w');
+            if ($fh_json !== false) {
+                fwrite($fh_json, PHP_EOL . "$data_string");
+                fclose($fh_json);
+            }
+        }
```
**Por qué es seguro:** Solo cambia el separador de ruta y agrega guarda ante `fopen → false`. El JSON escrito, la lógica de cuándo escribe y el contenido del `$data_string` son idénticos.

---

### 2 · Control de error en `curl_exec` de [ingresoFactura](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php#1764-2725) → [csv/modelo.php](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php) (~L 2610)

```diff
-        $data2 = json_decode($data, true);
+        /* Hardening Linux: control de error de red en curl */
+        if ($data === false) {
+            $curl_error = curl_error($ch);
+            error_log('[' . date('Y-m-d H:i:s') . '] CURL ERROR en ingresoFactura | Pedido: ' . $nro_pedido . ' | Error: ' . $curl_error);
+            $data2 = null;
+        } else {
+            $data2 = json_decode($data, true);
+        }

         $this->mensaje_api = $data2;
```
**Por qué es seguro:** Si no hay error de red, el flujo es **idéntico al original** (`json_decode → $data2`). Si hay error, antes el sistema hacía `json_decode(false)` produciendo `null` silencioso; ahora produce el mismo `null` pero con trazabilidad en el error_log del servidor. La lógica de `Succeeded`/`grabo` no se toca.

---

### 3 · Control de error en `curl_exec` de [ingresoArticulo](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php#1442-1650) → [csv/modelo.php](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php) (~L 1633)

Mismo patrón que el Cambio 2, aplicado al método [ingresoArticulo](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php#1442-1650). El error_log incluye el `$cod_articulo` para facilitar diagnóstico.

---

### 4 · Robustez en escritura de [detalle_proceso.txt](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/detalle_proceso.txt) → [csv/modelo.php](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/csv/modelo.php) (~L 898)

```diff
-                    if (file_exists("detalle_proceso.txt")) {
-                        $archivo = fopen("detalle_proceso.txt", "a");
-                        fwrite($archivo, PHP_EOL . "$mensaje_txt");
-                        fclose($archivo);
-                    } else {
-                        $archivo = fopen("detalle_proceso.txt", "w");
-                        fwrite($archivo, PHP_EOL . "$mensaje_txt");
-                        fclose($archivo);
-                    }
+                    /* Hardening Linux: fopen modo 'a' crea el archivo si no existe; guarda ante permisos */
+                    $fh_log = fopen("detalle_proceso.txt", "a");
+                    if ($fh_log !== false) {
+                        fwrite($fh_log, PHP_EOL . "$mensaje_txt");
+                        fclose($fh_log);
+                    } else {
+                        error_log('[' . date('Y-m-d H:i:s') . '] No se pudo escribir detalle_proceso.txt');
+                    }
```
**Por qué es seguro:** `fopen("...", "a")` en PHP crea el archivo si no existe, por lo que el `file_exists` original era redundante. El contenido escrito es idéntico. Se agrega `error_log` solo si falla la apertura.

---

### 5 · Backslash en lectura de directorio → [configuraciones/modelo.php](file:///d:/RXNAPP/Docker/www/rxnLadyApi/src/rxnLadyApiLinuxDockerizada/configuraciones/modelo.php) (L 81)

```diff
-        $ruta_de_la_carpeta = $this->leoParametroBd('RUTAXML') . '\\';
+        /* Hardening Linux: separador compatible con Linux y Windows */
+        $ruta_de_la_carpeta = rtrim($this->leoParametroBd('RUTAXML'), '/\\') . '/';
```
**Por qué es seguro:** `rtrim` normaliza el final del valor almacenado en BD (sea `/`, `\\`, o nada) y agrega exactamente un `/`. Idempotente en Windows y correcto en Linux.

---

## Puntos NO tocados

| Punto | Estado |
|---|---|
| Acumuladores `art_total_*` | **No tocado.** Interacción compleja entre pedidos; reset actual validado en producción. |
| `generoJsonCompleto()` | **No tocado.** Excluido explícitamente. |
| SQL con interpolación directa | **Reportado.** Fuera del alcance de esta etapa. |
| `closeCursor()` post-return | **Reportado.** Código muerto, no causa fallo. |
| Credenciales en `ConectarBase.php` | **Reportado.** Problema de seguridad para una etapa posterior. |
| `CURLOPT_VERBOSE true` en `ingresoArticulo` | **Reportado.** Activo en producción, genera output de debug. No se toca. |
